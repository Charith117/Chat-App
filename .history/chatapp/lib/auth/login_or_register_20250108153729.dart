import 'package:flutter/material.dart';

class LoginOrRegis extends StatelessWidget {
  const LoginOrRegis({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}